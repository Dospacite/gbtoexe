[package]
name = "gb-hw"
description = "Game Boy hardware model: PPU, APU, timers, joypad and cartridge mappers. No CPU — the CPU is recompiled."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
